name: My CI

on:
  push:
    branches:
      - main

jobs:
  CI-Rafael-Sanity:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: instalar Node
        uses: actions/setup-node@v4
        with: 
          node-version: 18
      
      - name: Instalar dependencias
        run: npm i

      - name: Run Test
              # Editar la ruta del test 
        run: npm run file cypress/e2e/Orange/GX-42582-orange-hrm-buzz-interact-with-post-by-shares-likes-or-comments.cy.js

        